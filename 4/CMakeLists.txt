cmake_minimum_required(VERSION 2.8)
project( scanImage )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable(scanImage my_scan_image.cpp )
target_link_libraries( scanImage ${OpenCV_LIBS} )
