cmake_minimum_required(VERSION 2.8)
project( DetailStitch )
find_package( OpenCV REQUIRED )
include_directories(${catkin_INCLUDE_DIRS} ${OpenCV_INCLUDE_DIRS} )
add_executable(DetailStitch DetailStitch.cpp )
target_link_libraries( DetailStitch ${catkin_INCLUDE_DIRS} ${OpenCV_LIBS} )
