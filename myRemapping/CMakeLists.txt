cmake_minimum_required(VERSION 2.8)
project(myRemapping)
find_package(OpenCV REQUIRED)
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable(myRemapping myRemapping.cpp)
target_link_libraries(myRemapping ${OpenCV_LIBS})
